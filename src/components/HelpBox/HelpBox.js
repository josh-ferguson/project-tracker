import React from "react";

import "./HelpBox.css";

export const HelpBox = () => {

    return(
        <div className="helpBox">
            <h3>No projects have been added yet!</h3>
            <p>Add a project and start planning! You can create a todo list of features by clicking on the desired project once a project has been added.</p>
        </div>
    );
}